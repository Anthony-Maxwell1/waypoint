TMDB Read-only access token goes in .env.local as such:
`tmdb-token={...}`

Filters for searching with builtin-search do not support the year filter for shows & movies combined search (not filtering by type.)
